Remember to clone with submodules.

## Building

### Prerequisites

-   Node.js
-   pnpm (`npm install --global pnpm`)
-   Rust/Cargo
-   wasm-pack (`cargo install wasm-pack`)

```
pnpm install
pnpm run dev
```
